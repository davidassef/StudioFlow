const withPWA = require('next-pwa')({
  dest: 'public',
  register: true,
  skipWaiting: true,
  disable: process.env.NODE_ENV === 'development' && !process.env.ENABLE_PWA,
  sw: 'sw-custom.js'
})

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  images: {
    domains: ['localhost'],
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  // Allow disabling static "export" mode during development so middleware and dev server work.
  // To force export mode (for static export builds), set FORCE_EXPORT=true in the environment.
  trailingSlash: true,
  distDir: 'out',
  experimental: {
    // Disable static optimization for all pages
    serverComponentsExternalPackages: [],
  },
  // Disable static generation completely
  generateBuildId: async () => {
    return 'build-' + Date.now()
  },
}

module.exports = withPWA(nextConfig)
