// Componentes de mapas
export { StudioMap } from './StudioMap';

// Hooks
export { useGeolocation, useDistance, useMapFilters } from './hooks';

// Tipos
export type {
  StudioLocation,
  MapCenter,
  MapBounds,
  GeolocationPosition,
  MapFilters,
  MapViewport
} from './types';

// Constantes
export { MAP_DEFAULTS, BRAZIL_BOUNDS, MAJOR_CITIES } from './types';